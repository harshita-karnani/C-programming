#include <stdio.h>
int main(void) {
	char a[100],b[100];
	int a1,a2;
	scanf("%s",a);
	scanf("%s",b);
	a1=strlen(a);
	a2=strlen(b);
	if(a1>a2)
	{
		printf("%s",a);
	}
		else if(a2>a1)
		{
			printf("%s",b);
		}
			else
			{
				printf("%s",b);
			}
	return 0;
}
